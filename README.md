# Quick start

1. Run the server with the http profile directly from Visual Studio, or with the following : 
```bash
althera-portal\app\server\Althera> dotnet run --launch-profile "http"
```
2. Run the client with npm run dev
```bash
althera-portal\app\clinic-client\portal> npm run dev
```